//Écrivez un programme Node.js qui utilise le module querystring pour analyser une chaîne de requête URL.const querystring = require('querystring');
const querystring = require('querystring');

const url = ('https://nodejs.org/dist/latest-v19.x/docs/api/querystring.html');

console.log(querystring.parse(url));


